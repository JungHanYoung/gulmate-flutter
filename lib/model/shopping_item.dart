class ShoppingItem {
  String title;
  String place;
  String author;
  DateTime date;
  bool isComplete;

  ShoppingItem({
    this.title,
    this.place,
    this.author,
    this.date,
    this.isComplete
  });
}